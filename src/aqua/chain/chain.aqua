module Chain declares ChainInfo

-- Data we need to poll new deals from aurora
data ChainInfo:
  -- Refers to which api endpoint to poll
  api_endpoint: string
  -- Chain contract address
  address: string
